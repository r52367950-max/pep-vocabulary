# 词迹代码扫描与修复报告

**检查日期：2026-09-14**  
**仓库：r52367950-max/pep-vocabulary**  
**基线提交：1386d0e8c80ee40b8cb13baf087d6beb9d19c336**  
**修复分支：fix/stability-security-performance**

## 结论

本轮检查覆盖应用源码、题目与调度逻辑、IndexedDB、JSON 备份、D1 同步、AI 配置与代理、Service Worker、生产构建和 npm 依赖。已修复可导致判分错误、学习记录不一致、并发覆盖、异常请求失控和离线资源缺失的问题，并清理未使用的旧界面及图片优化入口。

原有 22 项测试通过，但未覆盖本轮发现的主要行为缺陷。修复后共 **49 项自动测试通过**；类型检查、严格未使用声明检查、ESLint、数据审计、生产构建和实际 workerd 运行测试通过。依赖审计由 **24 项告警降至 0 项**。这是本次审计时已知依赖漏洞的结果，不代表软件不存在其他漏洞。

代码以独立分支和拉取请求交付；**未合并主分支、未部署线上站点、未读取或修改线上学习记录与真实 API Key**。

## 1. 修复清单

优先级含义：高＝可能破坏记录正确性、产生覆盖或涉及安全边界；中＝明显影响功能或可靠性。这里的应用问题优先级不等同于依赖公告的 CVSS 等级。

| 编号 | 优先级 | 问题及触发方式 | 修复与结果 | 主要文件 |
|---|---|---|---|---|
| R01 | 高 | 标准化函数删除中文；苹果和梨等不同选项可能同时变成空字符串，被判为相同答案 | 保留 Unicode 字母与数字，拒绝空答案；英文大小写和全角兼容保留 | `lib/questions.ts` |
| R02 | 高 | IndexedDB 请求成功便提前 resolve，之后事务仍可能中止 | 仅在 `transaction.oncomplete` 后报告成功；中止和同步异常均清理连接并拒绝保存 | `lib/storage.ts` |
| R03 | 高 | 卡片与复习事件分别写入；其中一次失败会造成调度状态和历史不一致 | 使用同一事务提交卡片与事件；重复事件写入会整体回滚；撤销也使用同一事务 | `lib/storage.ts`、`components/vocab-app.tsx` |
| R04 | 高 | 两个页面从同一旧状态评分；快速点击、滑动或按键可能重复提交 | 写入事务检查原卡片状态；界面使用互斥标志、当前题提交标记和禁用态，忽略重复键盘事件 | 同上、`components/console-study-session.tsx` |
| R05 | 高 | D1 先查版本、再覆盖写入；并发上传能同时通过检查。缺少 `baseRevision` 也可能覆盖已有数据 | 首次上传使用冲突拒绝；后续更新将用户身份、版本匹配与递增放在同一 SQL 语句中，冲突返回 409；版本必须为有效非负整数 | `app/api/sync/route.ts` |
| R06 | 高 | 恢复只检查四个数组，损坏的日期、调度字段、设置和重复 ID 可进入数据库 | 恢复前校验各记录、数值、日期、枚举及 ID；兼容 1.0 备份迁移；失败不替换现有数据；文件导入限制 25 MB 并确认替换 | `lib/storage.ts`、`components/vocab-app.tsx` |
| R07 | 高 | API 配置仅相信 Content-Length；AI 与同步接口先完整读入再检查；`null` JSON 可能触发属性访问异常 | 统一流式 JSON 对象读取，按实际 UTF-8 字节限制并取消超量读取；分别保留 4 KB、24 KB、5 MB 加封装余量的入口限制 | `lib/http.ts`、AI 与同步路由 |
| R08 | 高 | 同步缺少明确的跨源保护与私有缓存控制；部分异常直接返回数据库错误，配置写入失败可能成为未处理异常 | 同源及 Fetch Metadata 校验、JSON 类型约束、`no-store`、统一受控错误；数据库内部信息不再返回客户端 | `lib/http.ts`、`app/api/sync/route.ts`、`app/api/ai/config/route.ts` |
| R09 | 高 | Base URL 的尾点本地域名、单标签主机名及部分 IPv6 兼容/转换地址可绕过原地址检查 | 检查前去掉域名尾点，拒绝单标签及相关地址形式；保持上游请求禁止重定向 | `lib/assistant/core.ts` |
| R10 | 中 | 初次评分前人为创建卡片使撤销无法恢复“原本没有卡片”的状态；开始新会话后仍可撤销旧会话评分 | 调度显式接收词条 ID，保留真实的空前状态；新会话、导入、拉取和清空后重置撤销入口 | `lib/scheduler.ts`、`components/vocab-app.tsx` |
| R11 | 中 | 收藏/注释会创建未学习卡，原逻辑随即将其排除出新词并列为到期；旧页面修改注释还可能覆盖新调度状态 | 未学习卡仍可进入新词队列，不计入到期负担；元数据修改在事务中读取最新卡片，仅更新相关字段 | `lib/storage.ts`、`lib/scheduler.ts`、词库与主界面 |
| R12 | 中 | 中文或标点搜索去掉非拉丁字符后以空前缀匹配所有词 | 只有非空拉丁前缀才用于前缀匹配，中文仍按原内容检索 | `lib/lexicon.ts`、主界面 |
| R13 | 中 | 快速切换词条后，慢请求可能把旧词详情显示在新词下；专项训练题型被会话初始化覆盖 | 详情请求加入有效性检查，并按 ID 限制展示；题型作为会话建立参数传递，切题清空上一题作答状态 | `components/vocab-app.tsx` |
| R14 | 中 | 同义干扰项重复；造句目标词使用子串匹配，he 可匹配 theater | 选项按标准化文本去重，造句检查要求完整词或短语边界 | `lib/questions.ts` |
| R15 | 中 | 所有超过 14 天的到期任务被挤到预测末日；无效日期可访问不存在的数组项 | 只统计预测窗口内任务；逾期归今天；跳过无效、暂停、未学习卡；按本地日历日期归类 | `lib/scheduler.ts` |
| R16 | 中 | 无到期词时，“只复习”等模式仍回退创建新词练习 | 空计划显示说明，自由练习保留在词库入口 | `components/vocab-app.tsx` |
| R17 | 高/中 | 离线缓存遗漏实际 `/assets/` 目录；导航失败/登录跳转可能污染首页；更新清除同域其他缓存 | 预缓存构建脚本与 CSS；拒绝缓存私有 API、登录与 RSC 请求；仅成功且可缓存的响应写入；清理仅限本应用命名空间；更新等待旧页面退出 | `public/sw.js`、`build/sites-vite-plugin.ts` |
| R18 | 中 | 固定缓存版本和复用构建目录可能留下旧资源 | 由资源名称、词库内容和 SW 源码生成缓存指纹；验证构建前清理 `dist` | 构建插件、`scripts/build-verified.sh` |
| R19 | 中 | CSV/TSV 导出最多静默导出 2,000 条；公式型文本可能被表格软件执行 | 导出全部筛选结果；中和公式前缀，TSV 清理行列分隔字符；延迟回收下载链接 | `lib/export.ts`、主界面 |
| R20 | 中 | 数据库读取失败被当作“没有学习记录”；快速设置修改可能覆盖其他字段 | 初始化显式报告读取失败；设置更新串行读取最新值后保存；收藏、注释、清空和评分失败提供反馈 | `lib/storage.ts`、主界面 |

## 2. 依赖安全更新

基线 `npm audit`：1 项 critical、16 项 high、6 项 moderate、1 项 low，共 24 项。这些是 npm 对直接及传递依赖的告警项，含工具链风险与上游传播项，**不能理解为 24 个已经在本站复现的漏洞**。

| 依赖 | 原版本 | 修复版本 |
|---|---|---|
| Next.js / eslint-config-next | 16.2.6 | 16.3.5 |
| React / React DOM / react-server-dom-webpack | 19.2.6 | 19.2.8 |
| Vite | 8.0.13 | 8.3.0 |
| Cloudflare Vite 插件 | 1.37.1 | 1.54.9 |
| Wrangler | 4.92.0 | 4.131.2 |
| image-size | 锁文件原版本存在告警 | 通过 override 固定 2.0.4 |
| esbuild | 多个旧传递版本存在告警 | 通过 override 固定 0.28.2 |

其他允许范围内的传递依赖同时更新并锁定。保留 vinext 0.0.50，未仅为了清除告警而迁移到其新的 beta 主版本。esbuild 的覆盖涉及旧工具链依赖，因此额外执行了干净安装、生产构建和 Drizzle 迁移一致性检查。

相关公开依据包括：[React Server Functions 拒绝服务公告及 19.2.8 修复](https://github.com/advisories/GHSA-wx67-qw84-cm4g)、[image-size ICNS 解析拒绝服务公告](https://github.com/advisories/GHSA-w3rx-r6r6-pgpr)。完整对比计数见 `artifacts/repair/dependency-audit.json`。

## 3. 性能与代码清理

### 可复现实测

使用同一 Node.js 24.19.0 环境、同一 4,681 条词库；选项生成先预热，再进行 9 组、每组 100 次调用，报告组耗时中位数。脚本直接加载基线提交与修复后源码，不使用不同机器上的历史成绩。

| 指标 | 修复前 | 修复后 | 含义 |
|---|---:|---:|---|
| 100 次听音选择题选项生成中位耗时 | 53.35 ms | 21.62 ms | 本次微基准下降约 59.5% |
| 20 个并发 `loadLexicon()` 触发的请求数 | 40 | 2 | 索引和清单各请求一次，下降 95% |

测量结果不是全站响应速度或 iPad 性能承诺。原始数据见 `artifacts/repair/core-benchmark.json`，可运行 `npm run benchmark` 复现；耗时随设备及运行负载变化。

其余实现改进：词条 ID 到详情分片的映射只构建一次，移除每个 ID 逐项查找及固定 180 条分片假设；在途请求复用，失败后允许重试；到期词使用 ID 映射查找；仅进入词库页时预取列表详情；备份导出使用一个跨表只读事务，保证同一快照并减少连接次数。

删除未挂载的旧 `LexiconView`、旧 `StudySession`、随之失效的导入与帮助函数；移除没有业务使用且依赖不存在 IMAGES 绑定的图片优化入口。保留正在使用的页面、样式和数据迁移。Barlow 字体仅导入所需的 Latin 子集，中文与阅读字体保留。

干净构建后的资源对比如下，单位均为未压缩字节；不等同于首屏实际下载量。

| 产物 | 修复前 | 修复后 | 变化 |
|---|---:|---:|---:|
| 客户端 assets 总量 | 6,295,623 | 6,010,663 | −4.5% |
| CSS | 201,115 | 194,509 | −3.3% |
| JavaScript | 393,380 | 399,358 | +1.5% |
| assets 文件数 | 160 | 132 | −17.5% |

原始体积记录见 `artifacts/repair/build-size.json`。依赖安全升级和错误处理会增加部分 JavaScript，不能将请求次数下降与资源总量混为一谈。

## 4. 验证结果与复现方法

| 验证 | 结果与覆盖 |
|---|---|
| 干净依赖安装 | `npm ci --ignore-scripts --no-audit --no-fund` 成功 |
| 自动测试 | 49/49 通过，较原有 22 项增加 27 项 |
| 类型与静态检查 | TypeScript、额外未使用声明检查、ESLint、`git diff --check` 通过 |
| 数据审计 | 12 册教材、4,681 条发布候选；既有已披露缺口保持不变 |
| 生产构建 | vinext / Vite 构建及 Worker 导出、D1 元数据验证通过 |
| 实际 Worker 测试 | 使用 workerd + Miniflare 和临时本地 D1；首页、词库、SW、预缓存资源可读取；未认证接口 401、跨源配置 403；加密配置保存/删除成功；并发首次同步得到 200 和 409 |
| 迁移一致性 | `drizzle-kit check` 通过；本轮不新增数据库迁移 |
| 依赖漏洞审计 | 0 项已知告警 |

新增测试执行真实函数及数据库事务行为，包括：事务在请求成功后中止、重复事件回滚、两页冲突、首次评分与撤销、备份拒绝与迁移、中文评分与搜索、分片并发和失败重试、流式字节限制、地址过滤、私有缓存隔离、SQL 原子版本更新。D1 路由单测采用真实 SQLite 执行 Drizzle 生成的 SQL，另以实际 workerd 复核关键接口。

```bash
npm ci --ignore-scripts --no-audit --no-fund
npm test
npm run lint
npx tsc --noEmit --noUnusedLocals --noUnusedParameters
npx drizzle-kit check
npm audit
npm run benchmark
```

`npm test` 依次运行数据审计、类型检查、单元/回归测试、生产构建和本地 Worker 检查。`npm run test:runtime` 可在已完成构建后单独运行。测试身份、加密密钥和 D1 都是测试环境数据，不连接生产数据库或模型服务。

本次环境执行构建时设置了 `SITES_ENV_READY=1` 和指向仓库的 `SITES_PROJECT_ROOT`，使用已有运行环境而跳过旧包装脚本的 HOME 重定向。常规安装机器可以按上述项目命令运行。

## 5. 未验证事项与剩余边界

- **浏览器交互未完成实机复核。** 当前环境没有浏览器程序，Chromium 下载多次超时；没有把 HTTP、Node 或 SW 模拟测试冒充浏览器端到端测试。iPad/Safari 的触摸、下载、语音、缓存配额、UI 布局及评分按钮的实际点击行为仍需设备验证。
- **未用真实模型密钥测试外部服务。** AI 请求超时、错误映射、凭据边界使用现有和新增测试验证；实际服务商计费、模型可用性与外部网络连通性不在本次结论内。
- **身份仍依赖 Sites 网关。** 应用从平台注入的身份头读取用户；裸 Worker 若绕过受信网关直接公开，客户端伪造身份头属于部署边界风险。本轮没有更换身份体系或验证生产网关配置。
- **URL 文本校验不等于 DNS 出站隔离。** 本轮封堵了明确的地址格式绕过，但没有实现 DNS 解析后 IP 固定或域名允许列表；若部署允许不可信用户任意配置上游，仍应在部署层约束出站目的地。
- **离线详情按访问缓存。** 构建脚本和样式会预缓存；未访问的词条详情分片仍可能需要联网。缓存配额或浏览器主动清理可能影响离线可用性。新 SW 等旧页面关闭后激活。
- **已有数据不会凭空重建。** 之前已被错误评分或覆盖的记录，本轮不会推测修改。冲突会拒绝覆盖，不提供自动合并；应结合备份人工选择后续处理。
- **词库本身保留已有数据缺口。** 333 条缺少开放英文简义、153 条 IPA 不完整、7 个单元存在未解析字段，本轮未用生成内容填补这些来源问题。
- 原工具链仍提示 `@esbuild-kit` 包已弃用；弃用提示与漏洞告警不同。本轮通过锁文件和 override 消除已知告警，未进行无关的 ORM 大版本迁移。

## 6. 合并、发布与回退

通过拉取请求审查代码和报告后，可按原站点发布流程构建并部署；本轮仅交付代码。无需新增 D1 迁移，但目标环境仍须具有仓库已有的三份迁移、DB 绑定及安全加密配置。

发布前建议导出本机学习备份，在一台桌面设备和 iPad 上确认：中文错误选项判错、快速重复评分只写一次、撤销恢复前状态、导入损坏备份不改变记录、两设备上传冲突有提示，以及已访问词条的离线重开。新缓存激活需要关闭旧页面。

回退可撤销本修复提交并重新部署旧构建；本地数据 schema 仍为 1.1.0，未改变数据库表结构。回退同时会重新引入旧依赖告警与应用缺陷，应保留导出的学习备份。
