# 隐私说明

应用默认不要求账号，学习状态、复习事件、词单、设置和注释保存在当前浏览器 IndexedDB。用户可以导出 JSON 备份并自行保管，也可在明确确认后清空本机数据。

启用私有同步时，服务端从 Sites 身份头读取邮箱，只保存其 SHA-256 摘要作为键，不保存明文邮箱；D1 保存用户主动上传的 2 MB 以内备份和 revision。词库本身是静态公共资产，不在 D1 为每个用户复制。当前未配置 R2 或真人音频。

AI 开关默认关闭。模型密钥只允许作为 Sites Secret，或使用服务端 Secret 通过 AES-GCM 加密后保存在 D1；接口永不回显明文密钥。AI 请求只发送目标词 ID、必要的作答文本、有限掌握标签，以及服务端从正式发布索引重新解析的词库证据。AI 配置与请求内容不进入 IndexedDB、个人同步备份或导出文件，代理代码也不记录请求体、密钥或上游响应正文。未配置模型时，造句仅做本地结构检查；核心浏览、学习、复习、统计、导入导出和离线功能不依赖 AI。

应用不收集广告标识、位置、通讯录或与学习无关的个人信息。若未来增加外部模型、公开分享或真人音频，必须先更新本说明和权利审计，并在产生费用、公开可见或外部写入前获得用户确认。
