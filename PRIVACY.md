# 隐私说明

核心本机学习不依赖应用账号；托管站点本身可要求登录并限制访问范围。学习状态、复习事件、词单、设置和注释保存在当前浏览器 IndexedDB。用户可以导出 JSON 备份并自行保管，也可在明确确认后清空本机数据。

个人导入文章保存在独立的本机 IndexedDB，不包含在学习 JSON 备份或云端学习快照中，可逐篇导出 Markdown。文件解析与英文 OCR 在浏览器完成；首次扫描需要下载识别资源。

启用私有同步时，服务端从 Sites 身份头读取邮箱，只保存其 SHA-256 摘要作为键，不保存明文邮箱；D1 保存用户主动上传的 5 MB 以内备份和 revision。词库本身是静态公共资产，不在 D1 为每个用户复制。当前未配置 R2 或真人音频。

AI 开关默认关闭。用户可以在设置页提交 DeepSeek 或 OpenAI-compatible API Key：密钥通过 HTTPS 送达同源服务端，以独立部署密钥进行 AES-GCM 加密后保存到 D1。浏览器只接收“已配置/未配置”，密钥不会进入 localStorage、IndexedDB、JSON 备份、日志设计或 Git。Base URL 默认只允许 HTTPS，并拒绝账号信息、查询参数、重定向、本机、私网和特殊用途地址。用户可以单独移除服务端密钥；清空本机学习记录不会误删它。

解释、造句检查、练习生成和易混词辨析只发送当前词条 ID、必要作答文本、有限掌握标签，以及服务端从正式发布索引重新读取的词库证据；不上传整份词库、笔记或学习档案。模型输出不会写回正式词库。核心浏览、学习、FSRS 复习、统计、导入导出和离线功能不依赖 AI。

个人文章的 AI 分类默认关闭；用户主动开启时，标题与前 8,000 个字符会发送给已配置的 AI 服务商。未开启时可手动分类。测试连接会发送少量真实模型请求并可能产生费用，离线本机学习不调用这些服务。

应用不收集广告标识、位置、通讯录或与学习无关的个人信息。新增数据用途、服务商或公开分享时更新说明与权利依据，并取得相应授权；已经授权的同一操作无需逐步重复确认。
