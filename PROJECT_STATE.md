# PROJECT STATE

更新时间：2026-08-03 UTC

当前关卡：G 已完成私有部署；最终终版门槛因数据终审与设备复测仍未通过

词库版本：`1.0.0-rc.1`

用户数据 schema：`1.0.0`

## 关卡状态与证据

| 关卡 | 状态 | 证据 |
|---|---|---|
| A 仓库、环境、来源 | 完成 | 41 项 `source_manifest.json`；12/12 目标教材；`SOURCE_MANIFEST.md`；Sites D1 配置 |
| B 单元闭环 | 完成 | 必修一 Unit 1 可抽取、浏览、学习、评分、FSRS 复习、撤销和分析；对应 JSONL 与单元对账 |
| C 教材与课标覆盖 | 发布候选完成 | 高中 7 册、初中 5 册、官方高中课标附录 2,997 词头；逐册逐单元对账 |
| D 学习内核 | 完成 | `lib/scheduler.ts`、`lib/storage.ts`、`lib/questions.ts`；FSRS v6、一个主状态、六项能力、追加事件 |
| E 完整产品 | 完成 | 今日、词库、计划、学习、分析、数据；备份恢复、CSV/TSV、文章对齐、PWA |
| F 测试 | 主要门禁完成 | `npm test`、`npm run lint` 通过；10/10 自动测试；三种精确视口和主要真实流程通过；部署后设备验收项见 `TEST_REPORT.md` |
| G 私有部署与最终审计 | 部分完成 | [Sites 部署](https://pep-vocab-studio.namizore.chatgpt.site) 成功；策略为 `custom`，仅 owner 1 人、外部访客 0、群组 0；HTTPS 离线与实际身份同步仍需用户设备复测 |

## 最新可重复证据

- `node scripts/build-source-manifest.mjs` → 41 项来源，12 册教材。
- `node scripts/build-lexicon.mjs` → 7,938 原始记录、7,936 规范化记录、4,759 稳定词条、4,681 发布候选。
- `npm run data:audit` → 硬错误 0，状态 `release-candidate-with-declared-gaps`。
- `npm run test:unit` → 10/10 通过。
- `npm run lint` → 通过。
- `npm test` → 数据审计、10 项自动测试、生产构建和 Sites 产物验证全部通过。
- 真实浏览器 → 36/36 分层诊断完成且中途恢复有效；组合筛选、学习评分、完成页撤销、计划负担、分析、备份恢复、schema 拒绝、AI/D1 降级通过。
- 精确视口 → 390×844、820×1180、1440×900；真实截图位于 `artifacts/screenshots/`。
- Sites checkpoint → 提交 `475dfde` 构建和托管产物验证通过；生产部署终态 `succeeded`。
- Sites 访问策略 → `custom`；允许账户 1（owner），外部访客 0，授权群组 0；未执行公开访问变更。

## 数据数量

- 发布候选总数：4,681；应用中初中核心和高中七册范围独立筛选。
- 官方高中课标词汇附录：2,997 个词头行；等级分布 1,498 / 498 / 1,001。该数是对官方 PDF 文本行的可重复抽取，不将尾页“主要国家名称”表混入词表。
- 两册 OCR 教材：JH-7B、JH-8B；正式字段使用词表次序和 Apache-2.0 对照源交叉核验，OCR 不单独升格为正式来源。

## 已知问题（精确阻断“终版正式词库”门槛）

1. 333 个高中范围短语/专名没有许可清楚的开放英文简义；ID、词头、册次和单元样例见 `data/audit-summary.json`。影响：这些条目不能宣称达到高中 A 层终版。
2. 153 个词条缺一侧或两侧 IPA；应用以明确标注的系统 TTS 降级。影响：不满足全部 A/C 层音标终版门槛。
3. 7 个单元存在字段级未解析记录，详见 `data/audit-summary.json.gapSamples.unresolvedUnitRows`。影响：逐单元对账尚非零缺口。
4. B 层词族/搭配/辨析关系仍有 `provisional` 字段，未由人工编辑完成全量审校。
5. JH-7B、JH-8B 仍需第二名人工审校者逐页复核扫描版；当前只有 OCR + 词表对照 + 页图抽查。
6. 义务教育课标 2022 的英语分级词汇尚未形成与初中五册的完整差集；已核验文件来源，但未将模型推断补充混入正式包。
7. 近年山东适用新高考真题高频补充和常用课外补充尚未发布；因此这两个 UI 范围当前可筛选但数据为 0。

## 部署后仍须在用户设备复测

1. 真实 Sites 所有者身份下的 D1 写入、revision 冲突和 JSON 恢复。
2. HTTPS 首次加载后断网，应用壳、已缓存词库详情和到期复习是否继续工作。
3. 一台真实 iPad/手机上的屏幕阅读器、200% 缩放与系统减少动效。
4. 访问策略仍为仅所有者/管理员；未经用户另行确认不得公开。

## 下一步

1. 在站点所有者的浏览器执行首次加载 → 断网 → 到期复习 → 恢复联网；将结果补入 `TEST_REPORT.md`。
2. 在实际 Sites 身份下执行 D1 同步、revision 冲突、JSON 清空后恢复；若失败，保留本地数据并记录服务端日志。
3. 由第二名审校者处理 333/153/7 数据缺口和两册 OCR 逐页复核，再把词库从 `rc.1` 升为正式版本。
4. 在真实 iPad/手机完成屏幕阅读器、200% 缩放和系统减少动效验收。
